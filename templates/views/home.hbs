<!DOCTYPE html>
<html>
    <head>
        <title>Countries Information</title>
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        
        {{>header}}
        <div class="form">
            <form action="/" method="post">
                <input type="text" name="name" placeholder="enter country name">
                <button type="submit">Search</button>
            </form>
        </div>
        <div class="result">
            <img class="results" src={{flag}} alt={{altname}}>
            <p class="results">{{result}}</p>
            <p class="results">{{name}}</p>
            <p class="results">{{capital}}</p>
            <p class="results">{{region}}</p>
            <p class="results">{{subregion}}</p>
            <p class="results">{{population}}</p>
            <p class="results">{{area}}</p>
            <p class="results">{{currencycode}}</p>
            <p class="results">{{currencyname}}</p>
            <p class="results">{{currencysymbol}}</p>
            <p class="results">{{language}}</p>
        </div>
        {{>footer}}
        
    </body>
</html>